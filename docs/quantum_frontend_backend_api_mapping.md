# Quantum Frontend Backend API Mapping

Manual local browser verification uses `http://127.0.0.1:3000` for frontend work and `http://127.0.0.1:8000` for backend API work.

CI/headless smoke scripts may still set isolated `BACKEND_PORT` / `FRONTEND_PORT` when they need parallel-safe ports.
